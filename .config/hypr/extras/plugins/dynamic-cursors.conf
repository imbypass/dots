plugin {
    dynamic-cursors {
        enabled = yes
        mode = none

        shake {
            enabled = yes
            threshold = 8.0
            speed = 8.0
        }
        hyprcursor {
            enabled = yes
            nearest = yes
            fallback = clientside
        }
    }
}
