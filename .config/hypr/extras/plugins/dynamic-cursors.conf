plugin {
    dynamic-cursors {
        enabled = true
        mode = none

        shake {
            enabled = true
            threshold = 8.0
            speed = 8.0
        }
        hyprcursor {
            enabled = true
            nearest = true
            fallback = clientside
        }
    }
}
