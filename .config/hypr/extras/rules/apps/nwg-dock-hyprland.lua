hl.layer_rule({
    match = { namespace = "blur on" },
    -- TODO: manual review — unmapped layer rule: "match:workspace nwg-dock"
})

hl.layer_rule({
    match = { namespace = "ignore_alpha 0.5" },
    -- TODO: manual review — unmapped layer rule: "match:workspace nwg-dock"
})
