#! /usr/bin/env bash

# main() ?
if [[ $1 == "start" ]];then
    cd "/home/${USER}/Videos/Screen Recordings/" && wf-recorder -a -f "$2.mp4" > /dev/null
elif [[ $1 == "stop" ]];then
    kill -s INT $(pidof wf-recorder)
else
  echo "ok"
fi
